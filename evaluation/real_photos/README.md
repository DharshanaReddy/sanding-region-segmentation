Put real photos here (`.png`/`.jpg`) — e.g. a metal tray/sheet with a few
strips of tape or a marker scribble standing in for scratches/corrosion.
See `../README.md` for how these get used.

This directory is intentionally empty in version control (only this file
is tracked) — photos are yours to add locally.
